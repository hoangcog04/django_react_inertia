services:
  docs:
    image: django_inertia_local_docs
    container_name: django_inertia_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./django_inertia:/app/django_inertia:z
    ports:
      - '9000:9000'
    command: /start-docs
